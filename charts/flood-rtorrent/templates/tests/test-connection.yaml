apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flood-rtorrent.fullname" . }}-test-connection"
  labels:
    {{- include "flood-rtorrent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flood-rtorrent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

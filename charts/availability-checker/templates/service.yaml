apiVersion: v1
kind: Service
metadata:
  name: {{ include "availability-checker.fullname" . }}
  labels:
    {{- include "availability-checker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "availability-checker.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "samba.fullname" . }}
  labels:
    {{- include "samba.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 137
      targetPort: 137
      protocol: UDP
    - port: 138
      targetPort: 138
      protocol: UDP
    - port: 139
      targetPort: 139
      protocol: TCP
    - port: 445
      targetPort: 445
      protocol: TCP
  selector:
    {{- include "samba.selectorLabels" . | nindent 4 }}
